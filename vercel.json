{
  "version": 2,
  "framework": "vite",
  "installCommand": "npm install && npm install --prefix apps/dashboard",
  "buildCommand": "npm run build --prefix apps/dashboard",
  "outputDirectory": "apps/dashboard/dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD apps/dashboard"
}